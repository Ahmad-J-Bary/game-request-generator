/** Color settings used by Excel export styling (structurally compatible with UI ColorSettings) */
export interface ExcelColorSettings {
  levelBonus: string;
  levelNormal: string;
  purchaseRestricted: string;
  purchaseUnrestricted: string;
  headerColor: string;
  dataRowColor: string;
  incompleteScheduledStyle: string;
  completeScheduledStyle: string;
}
